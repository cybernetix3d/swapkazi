const express = require('express');
const router = express.Router();

// Placeholder for listing routes
router.get('/', (req, res) => {
  res.json({ message: 'Listing routes placeholder' });
});

module.exports = router;