/**
 * Maps configuration
 */

// We no longer need to store the Google Maps API key here
// It's now securely stored on the server side

// Default location (Cape Town)
export const DEFAULT_LOCATION = {
  latitude: -33.9249,
  longitude: 18.4241,
  latitudeDelta: 0.0922,
  longitudeDelta: 0.0421,
};
